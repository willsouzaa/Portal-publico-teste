"use client";
import { LeadCaptureModal } from "@/components/LeadCaptureModal";

export default function LeadModalClientWrapper() {
  // Wrapper mounts the modal. Debug UI removed per request.
  return <LeadCaptureModal />;
}
