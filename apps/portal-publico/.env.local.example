# Example .env.local for portal-publico
# COPY this file to .env.local and replace the placeholders with your real values.

# Google Places API key (keep private, do NOT commit to git)
# Generate in Google Cloud Console -> APIs & Services -> Credentials
GOOGLE_PLACES_API_KEY=YOUR_GOOGLE_PLACES_API_KEY_HERE

# Google Places Place ID for your business
# Find it using the Place ID Finder or the API examples below
GOOGLE_PLACES_PLACE_ID=YOUR_GOOGLE_PLACES_PLACE_ID_HERE

# Optional: debug flag for opening lead modal in dev
# NEXT_PUBLIC_DEBUG_OPEN_MODAL=1
